<resources>

    <!-- Base application theme. -->
    <style name="Theme.KorfbalStats" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>
    <style name="scorebutton_style">
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
    </style>
    <style name="player_name_style">
        <item name="android:textSize">24sp</item>
        <item name="android:fontFamily">@font/roboto_regular</item>
    </style>
    <style name="cancel_button_style" parent="@style/Widget.MaterialComponents.Button.OutlinedButton">
        <item name="strokeColor">@color/colorAccent</item>
        <item name="iconTint">@color/colorAccent</item>
    </style>

    <style name="list_view_item">
        <item name="android:layout_height">@dimen/lvItemHeight</item>
        <item name="android:clickable">true</item>
    </style>

    <style name="header">
        <item name="android:textSize">48sp</item>
        <item name="android:fontFamily">@font/roboto_thin</item>
        <item name="android:color">@color/colorPrimary</item>
    </style>

    <style name="homeBtn" parent="Widget.MaterialComponents.Button">
        <item name="android:textSize">18sp</item>
<!--        <item name="android:fontFamily">@font/roboto_regular</item>-->
        <item name="strokeWidth">1dp</item>
        <item name="strokeColor">@color/colorPrimary</item>
    </style>

</resources>
